
---

## 11. 다음 관측 계획 (2026-08-27 아침, 예약시각 06:00)

### 11.1 준비된 상태

| 항목 | 값 |
|---|---|
| 서버 | 예약시각 **정각 1회만** 발사(재시도 제거, §11.5) |
| 기기 | TestFlight 새 빌드 설치(라벨 `1.2.7+42`이나 내용은 `8e39497`+`1036e23` 포함), 앱 1회 실행 후 스와이프 킬, 화면 off, **USB 미연결**, 핫스팟 전용 |
| 예약시각 | 06:00 / `supports_push_heartbeat=true` / G+S=true |
| 직전 heartbeat | 08-26 06:09:32, key `2026-08-26_10:00` (옛 예약시각 — 이번에 고쳐진 부분) |

### 11.2 판정 대상 3가지

1. **키가 `2026-08-27_06:00`인가** → `8e39497`(예약시각 즉시 App Group 반영) 검증
2. **전송 성공 시 06:15 폴백 알림이 뜨지 않는가** → `1036e23`(발화한 알림 제거) 검증
3. **서버 로그 발사가 06:00 한 줄뿐인가** → 재시도 제거 확인

### 11.3 관측 순서 (앱을 열면 그날 검증이 끝난다)

1. 폰을 건드리지 않은 채 **보호자 폰**의 알림을 본다 — 이게 heartbeat 서버 도착의 직접 증거다
2. 한 시간 정도 기다려도 없으면 아이폰 **화면만 터치**(잠금화면 확인). 이 순간 기기가 깨어나며 보관돼 있던 푸시가 전달될 수 있다 — 전달까지 몇 분 걸릴 수 있으므로 5분 정도 본다
3. 그래도 없으면 화면을 연다
4. **앱 실행은 마지막**

### 11.4 터치 없이 쓰는 1차 판별 — 소리 유무

이 푸시는 사일런트가 아니다. 원본은 소리가 나고, **확장이 성공했을 때만** 무음(`.passive`)으로 바뀐다.

- **소리가 났다** → 원본이 표시된 것 = 확장 미실행 또는 전송 실패
- **조용하다** → 확장 성공(무음 전환) **또는** 아직 아무것도 도착 안 함 → 보호자 폰으로 구분

### 11.5 ⚠️ 재시도를 다시 넣지 말 것

+5분·+10분 재시도를 넣었다가 되돌렸다(2026-08-26). 푸시는 잠든 기기를 **깨우지 못한다** —
APNs가 보관했다가 기기가 스스로 깨어날 때 전달되는 것이라, 몇 번을 더 보내도 깨어나는
시점이 달라지지 않는다. 유일한 이득이던 보관 슬롯 재확보도 마지막 재시도 뒤에 다른 푸시가
하나만 더 오면 무력하고, 그 덮어쓰기 가설 자체가 **아직 확정되지 않았다**.
사용자 유도는 오프라인 폴백(+15분)이 이미 보장한다.

### 11.6 참고

- **08:00에 보호자 "주의" 경고가 나갈 수 있다** — 예약시각 +2h 미수신 체크. 테스트로 인한 것.
- 진단(발송 없이 상태만):
  ```bash
  cd anbucheck-server
  curl -s -X POST "$BASE/api/v1/admin/ios-heartbeat-trigger?dry_run=true" \
    -H "X-Admin-Key: $(grep '^ADMIN_SECRET_KEY=' .env | cut -d= -f2-)" | python3 -m json.tool
  # schedule / supports_push_heartbeat / last_seen / recent_logs(키·시각) 확인
  ```
- ⚠️ Railway 로그는 **배포 시작 시점부터만** 보인다. 읽기 전에 상단
  `You reached the start of the range`를 확인하고, 그 이전은 HISTORY의 이전 배포에서 볼 것.
- 정시성은 기대하지 않는다. 얻은 것은 "정시 전송"이 아니라 **"사용자가 아무것도 하지 않아도
  언젠가는 자동 전달된다"** 이며, 그 "언젠가"가 얼마인지가 이번 관측의 핵심 값이다.
