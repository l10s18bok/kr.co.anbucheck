<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- android:screenOrientation="portrait" 설정 시 AAPT가 자동으로
         <uses-feature android:name="android.hardware.screen.portrait" required="true">를
         암묵 추가해 세로 화면을 지원하지 않는 기기(차량 인포테인먼트, 일부 TV/태블릿)를
         스토어에서 자동 제외시킨다. 이 앱은 UI 레이아웃 목적으로만 세로 고정할 뿐 기기
         호환성 요구사항이 아니므로 명시적으로 required=false로 해제한다. -->
    <uses-feature android:name="android.hardware.screen.portrait" android:required="false"/>
    <!-- 알림 권한 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 걸음수 감지 (Android 10+) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <!-- 재부팅 후 예약 알림 복구 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Doze 프로브 리시버가 onReceive 반환 후에도 프로브를 이어가기 위해 필요.
         normal 권한이라 설치 시 자동 승인 — 사용자 프롬프트도 Play 정책 이슈도 없다.
         ⚠️ SCHEDULE_EXACT_ALARM은 선언하지 않는다. setAndAllowWhileIdle(부정확)은
         권한이 필요 없고, 그 권한은 Android 14+ 기본 거부 + Play 정책상
         알람시계·캘린더 앱 전용이라 선언하면 정당화 서류를 요구받는다. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- AdMob 광고 ID (Android 13+ 필수 선언) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    <!-- 긴급 도움 요청 시에만 1회 위치 수집 (포그라운드) — 백그라운드 위치 선언 금지 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

<application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <!-- Android 16(targetSdk 36)은 sw≥600dp 대형 화면(펼친 폴더블·태블릿)에서
             screenOrientation="portrait"를 무시해 가로 진입을 허용한다. 이 앱의 세로 고정은
             PRD-FrontEnd §6에 기록된 의도적 결정이므로 opt-out으로 기존 동작을 유지한다.
             ⚠️ 이 property는 targetSdk 37에서 완전히 제거된다. build.gradle.kts가
             targetSdk를 핀하지 않고 flutter.targetSdkVersion을 따라가므로, Flutter 업그레이드로
             targetSdk가 37이 되는 순간 경고 없이 무효화된다(그때는 반응형 대응 또는 targetSdk 핀). -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
            android:value="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <!-- FCM 기본 알림 채널 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="anbu_alerts" />
        <!-- FCM 기본 알림 아이콘 -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <!-- AdMob App ID (실 게재용 — 계정 pub-5477604587043205) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5477604587043205~8973493189"/>
        <!-- Google Maps API 키 — local.properties의 MAPS_API_KEY를 manifestPlaceholders로 주입 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}"/>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 구형 Android 폰 호환성: Impeller 비활성화
             화면 off/on 시 surface 재생성에서 구형 GPU가 Impeller 복구 실패 → 앱 UI 공백 이슈 회피 -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- flutter_local_notifications: 예약 알림 발동 리시버 -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <!-- flutter_local_notifications: 재부팅 후 예약 알림 복구 리시버 -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Doze 관통 알람 실측 프로브 (Phase 1, 네이티브 전용 — Dart를 실행하지 않는다).
             exported=false: 외부에서 부를 필요가 없다. adb로 강제 발화시킬 일이 있으면
             `am broadcast`가 아니라 앱을 열어 armInMinutes로 무장한다. -->
        <receiver
            android:name=".DozeAlarmProbeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <!-- Play 자동 업데이트는 앱을 열지 않아도 일어나고 그때 알람이 유실된다.
                     이게 없으면 업데이트된 기기는 사용자가 앱을 다시 열 때까지 알람이 없다. -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
